import React from "react";
import styled from "styled-components";

const MovieList = () => {
  return <MovieListTitle>Movie list!</MovieListTitle>;
};

const MovieListTitle = styled.p`
  margin: 13px;
  color: palevioletred;
`;

export default MovieList;
